import { Common } from './common';

export type appInfo = {
  name: string;
  appDescription: string;
  iconPath: string;
  tags: string[];
  webUrl: string;
  githubUrl: string;
  appStoreUrl: string;
  zasetuReason: string;
};
